import { Base, TeamMember } from '../';
import { TeamMemberPost } from '../utils/Typings';
export default class TeamMemberManager extends Base {
    getAll(): Promise<TeamMember[]>;
    get(TeamMemberID: string): Promise<TeamMember>;
    add(data: TeamMemberPost): Promise<any>;
    delete(TeamMemberID: string): Promise<any>;
    private _check;
}
