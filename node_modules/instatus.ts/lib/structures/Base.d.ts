import { InstatusClient } from '../';
import { Method } from 'axios';
/**
 * Base class
 *
 * @export
 * @class Base
 */
export default class Base {
    client: InstatusClient;
    /**
     * @param {InstatusClient} client The client that instantiated this
     * @memberof Base
     */
    constructor(client: InstatusClient);
    /**
     * Makes a request to the API
     *
     * @param {Method} method HTTP Method
     * @param {string} uri URI
     * @param {object} [data] Request date
     * @return {*}  {Promise<any>}
     * @memberof Base
     */
    request(method: Method, uri: string, data?: object): Promise<any>;
}
